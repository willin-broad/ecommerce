# Grafana Dashboards

Place Grafana dashboard JSON exports here.

## Recommended dashboards to set up:
- Services Overview (HTTP request rate, error rate, latency)
- Node Resource Usage (CPU, memory per pod)
- RabbitMQ Queue Depth
- PostgreSQL Connections

Import JSON files via: Grafana UI → Dashboards → Import → Upload JSON
